// Armazena um número inteiro
let numero = 14;

// Utiliza o operador % para verificar o resto da divisão por 2
if (numero % 2 === 0) {
    console.log("O número " + numero + " é par.");
} else {
    console.log("O número " + numero + " é ímpar.");
}